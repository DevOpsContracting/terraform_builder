regions_info = { for v in [ "primary", "secondary" ] : v => {
	dns_friendly_name = "east"
	location = "East US"
	short_name = "use"
} }

{
  "primary": {
    "dns_friendly_name": "east",
    "location": "East US",
    "short_name": "use"
  },
  "secondary": {
    "dns_friendly_name": "east",
    "location": "East US",
    "short_name": "use"
  }
}


my_active_regions = merge(
	{ "primary" = eastus,}
	(lookup(local.my_env, "geo_active", false) ? { "secondary" = centralus } : {}),
)WS